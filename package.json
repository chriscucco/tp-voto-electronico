{
  "name": "tp-voto-electronico",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "@truffle/contract": "^4.5.14",
    "@truffle/hdwallet-provider": "^2.0.8",
    "assert": "^2.0.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "https-browserify": "^1.0.0",
    "knex": "^2.0.0",
    "os-browserify": "^0.3.0",
    "path-browserify": "^1.0.1",
    "pg": "^8.7.3",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-scripts": "5.0.1",
    "stream-browserify": "^3.0.0",
    "stream-http": "^3.2.0",
    "url": "^0.11.0",
    "web-vitals": "^2.1.4",
    "web3": "^1.7.4"
  },
  "scripts": {
    "start-client": "GENERATE_SOURCEMAP=false react-app-rewired start",
    "start-server": "node ./server/server.js",
    "start-dev": "npm run build && npm run migrate && node ./server/server.js",
    "start": "npm run migrate && node ./server/server.js",
    "migrate": "node scripts/migrate.js",
    "build": "NODE_OPTIONS=--max_old_space_size=4096  GENERATE_SOURCEMAP=false  react-app-rewired build",
    "eject": "react-app-rewired eject",
    "test": " npm run test:unit && react-app-rewired test --watchAll"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "react-app-rewired": "^2.2.1",
    "jest": "^28.1.1",
    "mock-knex": "^0.4.11"
  }
}
